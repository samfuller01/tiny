trigger:
  - master

pool:
  vmImage: 'ubuntu-latest'

steps:
  - task: NodeTool@0
    inputs:
      versionSpec: '10.x'
    displayName: 'Install Node.js'

  - script: |
      yarn
      yarn test
      yarn lint
    displayName: 'yarn installs dependencies, tests, and lints the code'
